#include <stdlib.h>

struct Point {
    int x;
    int y;
};

int area(struct Point *p1, struct Point *p2);

struct BabaJoe {
    int age;
    int weight;
    int tail_lenght;
};

int fibonacci(int n);
